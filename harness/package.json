{
  "name": "@ssenrah/harness",
  "version": "0.1.0",
  "description": "Agent transparency layer — event capture, redaction, and CLI for Claude Code hooks",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "ssenrah": "dist/cli.js",
    "ssenrah-hook": "dist/hook.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "hook": "tsx src/hook.ts",
    "cli": "tsx src/cli.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  },
  "dependencies": {
    "better-sqlite3": "^11.0.0"
  }
}
